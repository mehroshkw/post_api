# post_api
Following Flutter project contains Register and login-in with POST API and storing Multi-Media i.e., Images and Audios  to the database.
Here I've only included the code files, android in which I took the storage permisions and pubspec.yaml for the dependencies I used. 
